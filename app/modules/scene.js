App.modules.Scene = Ext.extend(Core.BaseClass, {
    port: 80,
    constructor: function(port) {
        var me = this;
        App.modules.Scene.superclass.constructor.apply(me, arguments);
        
        
    }
});